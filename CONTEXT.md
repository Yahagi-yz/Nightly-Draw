# CONTEXT — 当前共识与状态

更新日期：2026-09-09（北京时间）。项目名称“每日抽卡 / Nightly Draw”，仓库 `Yahagi-yz/Nightly-Draw`。

## 用户确认的需求

1. 睡前抽卡体验，不是每日新闻简报。
2. 内容包括世界各地已知故事、人物故事和历史名城；可以原创，但“仅原创奇幻”不是当前要求。
3. 每次独立可读，主题、人物或地点可以松散关联。
4. 形式可为文字、图片或图文，不要求每晚必须生图。
5. 每天北京时间 22:00 主动提供完整内容，不需要先回复。
6. 建立独立 GitHub 项目，为以后 App 开发积累内容与产品基础。
7. 用户已完成仓库创建，当前继续初始化该仓库，不重新询问仓库地址或创建意愿。

## 已核实事实

- GitHub 接口已读取 `Yahagi-yz/Nightly-Draw`：当前公开，默认分支 `main`，初始提交 `cddde7b51141c4658922fbfc2f1ab9c3a39d3b66`；初始化前仅有 README。
- 真实项目主线 Issue #1 已创建：https://github.com/Yahagi-yz/Nightly-Draw/issues/1 。
- 本仓库沿用用户已有公开性，不修改可见性；这不等于已选定代码许可证或第三方素材授权。
- 当前会话有现有仓库文件与 Issue 的写入能力，但未提供能创建定时内容推送的工具；可用扩展检索没有解决这一缺口。
- `config/schedule.json` 仍是未启用的期望配置，没有任务 ID、下一次运行时间或设备接收凭据。
- 不用其他仓库、循环日历提醒或 GitHub Issue 通知代替本项目内容推送；不启用付费资源，不提交凭据。

## 初始化范围

提交目标、共识、内容规范、数据契约、3 张带出处的结构样卡、单文件预览、测试和任务指令。更新旧包中“仓库未创建”的状态；修复测试中把“当前目录没有 .git”当作前提的问题。没有进行 App 正式实现或部署。

## 可逆建议，尚非已定架构

先验证每日内容，再验证交付闭环，之后考虑移动网页 / PWA 或原生 App。图片不可用时可以降级文字；生产字数、三类比例与图片来源尚未锁定。

## 后续真正需要取舍的事项

长期推送渠道及所需授权、图片来源、收藏与历史回看、重抽规则、运行费用、代码许可证。技术栈不是用户现在必须回答的问题。眼前推送阻塞是工具能力缺口，不是用户没有说明时间或内容。

## 文档边界

MISSION 保存目标；本文件保存共识、状态与待决策事项；重要架构选择发生后再写 ADR。后续以真实工具结果和当前仓库为准，不以旧会话或旧包状态覆盖最新事实。
