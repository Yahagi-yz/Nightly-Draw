# Agent 工作约定

先读取 MISSION.md、CONTEXT.md、ROADMAP.md 和 docs/；以最新用户确认及真实仓库内容为准。

- 事实由 Agent 自查；涉及公开性、预算、渠道和产品体验的重大取舍再交用户。
- 不把所有待决策项一次性抛给用户。此阶段不需要强行锁定 App 技术栈。
- 史实、传说、文学改编和原创明确区分；每张非原创题材卡保留来源。
- 不用仅含提示词或坏链接的“图片卡”充数；不得把生成图当作历史照片。
- 不提交密钥、令牌、设备推送凭据或私人联系方式。
- 不创建付费资源，不擅自公开仓库，不修改用户其他项目。
- 修改数据后运行 `python scripts/validate.py` 与 `python -m unittest discover -s tests -v`。
- 文档里写了定时配置不代表已创建定时任务；只有真实工具返回才能更新线上状态。
- 禁止声称未来会自行后台执行；只有实际部署且验证的调度系统才能承诺其配置行为。
- 本包未包含真实上线服务，preview.html 是本地样卡预览，不是 MVP 上线证明。
