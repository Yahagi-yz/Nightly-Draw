# 初始化验证记录

日期：2026-09-09（北京时间）。本记录针对本次入库文件，旧包的验证结论不代替当前复测。

## 本次已执行

- `python scripts/validate.py`：通过，3 张结构样卡，期望时区 Asia/Shanghai、时间 22:00，任务未启用。
- JSON Schema Draft 2020-12：schema 本身与 3 张卡片通过，启用 URI/日期格式检查。
- `python -m unittest discover -s tests -v`：13 项通过。
- 修复旧测试对项目根目录不存在 `.git` 的错误假设；无 Git 与已有 Git 目录两种预演场景均检查不改文件。
- Chromium 本地页面：三类切换、下一张循环、来源展开/收起、来源链接、选择状态与控制台错误检查通过。
- 页面内嵌三张样卡与 `content/cards/` 的 JSON 内容完全一致。
- 390 像素移动视口与 1200 像素桌面视口无横向溢出，已保存本地截图检查。
- 已通过 GitHub 接口核对目标仓库、初始提交与真实 Issue #1。

## 未执行 / 未验证

没有创建定时任务、发送设备通知或部署 App；没有验证正式生产卡池规模、第三方图片权利或商业发布授权。本次未重复原初始化包的外部来源调研；出处登记继续保留原访问日期。

浏览器检查基于本地单文件预览，不代表公网托管、真机或跨浏览器验收。最终远端提交与回读核对结果记录在 Issue #1，不把本记录当作自动推送证明。
